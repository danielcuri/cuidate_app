import { environment } from '../config/environment';

export const api_url = environment.api_url;
export const api_url_learning = environment.api_url_learning;
export const api_url_medical = environment.api_url_medical;
